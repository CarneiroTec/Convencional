#Inclua <Convencional.int>

Inteiro Início() {
    Dados bloco;

    GerenciadorDados.inicialize(&bloco);

    bloco.tipo = 1;
    bloco.arquivo = 3;

    GerenciadorDados.limpe(&bloco);

    Retorne 0;
}
